#include <stdio.h>
int main()
{
    int n1, n2, n3, n4, n5, n6, n7, n8, n9;
    int i, j, k, l;
    while(scanf("%d %d %d %d %d %d %d %d %d", &n1, &n2, &n3, &n4, &n5, &n6, &n7, &n8, &n9)==9)
    {
        printf("%dx^8 + %dx^7 + %dx^6 + %dx^5 + %dx^4 + %dx^3 + %dx^2 + %dx + %d\n", n1, n2, n3, n4, n5, n6, n7, n8, n9);]
        for(i=8; i>=0; i++)
        {

        }
    }
    return 0;

}
